services:
  - type: web
    name: moneymb-backend
    env: node
    plan: free
    buildCommand: npm install && npm run build:shared && npm run build:backend
    startCommand: node packages/backend/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
